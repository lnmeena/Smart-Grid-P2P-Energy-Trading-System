[project]
name = "p2p-energy-trading"
version = "0.1.0"
description = "Peer-to-peer energy trading simulator for IEEE 14-bus dataset."
readme = "README.md"
authors = ["Generated by GitHub Copilot"]
requires-python = ">=3.10"

[project.dependencies]
pandas = "*"
networkx = "*"
pytest = "*"

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
python_files = ["test_*.py"]
